import React from 'react';

// Component neutralized to remove AI dependencies.
// Keeps interface to prevent breaking imports if used elsewhere.
interface AITutorProps {
  currentTopic: string;
  context: string;
}

export const AITutor: React.FC<AITutorProps> = () => {
  return null;
};